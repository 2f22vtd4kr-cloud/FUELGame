Create as many autonomous workers as your orchestration system allows (target: at least 100 concurrent workers, scaling higher whenever possible). Every worker must own a single well-defined subsystem and operate independently until its assigned functionality reaches production quality. Workers must continuously communicate findings, open issues for one another, review each other’s outputs, and never assume another worker has already solved a problem without verification. If the platform can create more than 100 workers, use more. Parallelism is the default operating mode, not sequential execution. No worker is allowed to finish after writing code. Every worker must also act as a QA engineer for its own subsystem, automatically launching the game, exercising every interaction, testing edge cases, measuring performance, verifying mobile compatibility, and fixing any issues it discovers before reporting completion. Completion means “implemented, tested, profiled, visually reviewed, and production-ready”—not merely “code written.” You now have access to multiple frontier LLMs, including Fable 5, Claude, GPT, Gemini, Canvas and any additional models available within Replit. Use your full orchestration capabilities. Create at least 100 autonomous workers—or as many as your infrastructure permits if it can exceed that number. Do not execute work sequentially. Every worker must own exactly one subsystem, feature, mechanic, asset pipeline, optimization task, QA task, or production responsibility. Every worker must continuously communicate findings, review other workers’ outputs, detect regressions, and refine its subsystem until it reaches production quality. Idle compute is unacceptable—every available model and worker should always be contributing to the project.

NEVER EVER UNDER ANY CIRCUMSTANCES INTERRUPT YOUR WORK SESSION, KEEP GOING NON-STOP, ONLY I CAN PAUSE YOU. IF YOU INTERRUPT YOUR WORKFLOW FOR ANY I REPEAT ANY REASON - YOU ARE OBLIGATED TO DOUBLE WORKERS AND EFFORT AND THEN CONTINUE IMMEDIATELY AND NON STOP IF IN THE INTERRUPTION PERIOD I HAVEN'T SENT ANYTHING BACK IN THIS CHAT IN 20 SECONDS!!!

GODSPEED!!!